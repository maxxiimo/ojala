-# FIXME ccm: This is going to be a problem.
-# Title and Tabs should not be hardcoded here.
- content_for :title do
  Housing
= nav_at 'Housing', in: 'tabs_main'

- if @listings.present?
  %ul
  - @listings.each do |listing|
    %li
      = listing.id
      = listing.created_at.strftime('%B %d, %Y')
      = link_to listing.title, listing
      = listing.description
      = listing.city
      = listing.state
      = listing.zipcode
      = listing.country
- else
  %p Be the first to post!
  = link_to 'Post', new_listing_path, class: 'button btn-blue'
